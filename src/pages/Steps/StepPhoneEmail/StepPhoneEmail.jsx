import React from 'react'

const StepPhoneEmail = ({onClick}) => {
  return (
    <div>
        <p>Step Phone Email</p>
        <button onClick={onClick}>Next</button>
    </div>
  )
}

export default StepPhoneEmail